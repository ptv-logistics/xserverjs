{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch index.html (without sourcemaps)",
            "type": "chrome",
            "request": "launch",
            "file": "${workspaceRoot}/lrm-xserver2/index.html"
        }
    ]
}